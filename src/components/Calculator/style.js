import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
    questionBox: {
    },
    questionText: {
        paddingTop: '5px',
        paddingBottom: '5px',
        paddingRight: '5px',
    },
    questionInput: {
        paddingTop: '10px'
    },
}));