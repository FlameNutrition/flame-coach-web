import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
    root: {
        height: "auto",
        marginBottom: '24px'
    },
    content: {
        height: "100%"
    },
    title: {
        marginTop: '15px',
        marginLeft: '15px'
    }
}));